maior = 0
menor = 0

for n in range(1, 11):
    n = int(input('Diga um número:\n'))
    if n > maior:
        maior = n
    if n < menor:
        menor = n

print(f'O maior número recebido foi {maior}\nO menor número recebido foi {menor}')
